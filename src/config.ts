const config = {
  api: {
    countdown: `/countdown`,
  },
  routes: {
    home: `/`,
    page1: `/page1`,
    // ToDo: Remove before deploying.
    playground: `/playground`,
  },
};

export default config;
